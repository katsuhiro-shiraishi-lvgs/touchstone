'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const index = require('./node_modules/.pnpm/klona@2.0.6/node_modules/klona/full/index.js');
const guard = require('./packages/utilities/core/src/guard.js');

function structuredClone(v) {
  return index.klona(v);
}
function toEvent(event) {
  const obj = guard.isString(event) ? { type: event } : event;
  return obj;
}
function toArray(value) {
  if (!value)
    return [];
  return guard.isArray(value) ? value.slice() : [value];
}
function isGuardHelper(value) {
  return guard.isObject(value) && value.predicate != null;
}

exports.isGuardHelper = isGuardHelper;
exports.structuredClone = structuredClone;
exports.toArray = toArray;
exports.toEvent = toEvent;
