import { klona } from './node_modules/.pnpm/klona@2.0.6/node_modules/klona/full/index.mjs';
import { isArray, isString, isObject } from './packages/utilities/core/src/guard.mjs';

function structuredClone(v) {
  return klona(v);
}
function toEvent(event) {
  const obj = isString(event) ? { type: event } : event;
  return obj;
}
function toArray(value) {
  if (!value)
    return [];
  return isArray(value) ? value.slice() : [value];
}
function isGuardHelper(value) {
  return isObject(value) && value.predicate != null;
}

export { isGuardHelper, structuredClone, toArray, toEvent };
