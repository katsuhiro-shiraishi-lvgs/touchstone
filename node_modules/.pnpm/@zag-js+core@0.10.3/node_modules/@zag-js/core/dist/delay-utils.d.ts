import type { Dict, StateMachine as S } from "./types";
/**
 * Delay can be specified as:
 * - a string (reference to `options.delays`)
 * - a number (in ms)
 * - a function that returns a number (in ms)
 *
 * Let's resolve this to a number
 */
export declare function determineDelayFn<TContext extends Dict, TEvent extends S.EventObject>(delay: S.Delay<TContext, TEvent> | undefined, delaysMap: S.DelayMap<TContext, TEvent> | undefined): (context: TContext, event: TEvent) => number | undefined;
