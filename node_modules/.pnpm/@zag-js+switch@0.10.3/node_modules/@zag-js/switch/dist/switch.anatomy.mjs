import { createAnatomy } from '@zag-js/anatomy';

const anatomy = createAnatomy("switch").parts("root", "input", "label", "control", "thumb");
const parts = anatomy.build();

export { anatomy, parts };
