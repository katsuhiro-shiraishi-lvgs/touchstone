import { createAnatomy } from '@zag-js/anatomy';

const anatomy = createAnatomy("pagination").parts(
  "root",
  "pageTrigger",
  "ellipsis",
  "prevPageTrigger",
  "nextPageTrigger"
);
const parts = anatomy.build();

export { anatomy, parts };
