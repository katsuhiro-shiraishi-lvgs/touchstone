import type { JSX } from 'react/jsx-runtime';
import { type ReactNode } from 'react';
import { type Assign } from '../types';
import { type UseSelectProps, type UseSelectReturn } from './use-select';
export type SelectProps = Assign<UseSelectProps, {
    children?: ReactNode | ((state: UseSelectReturn) => ReactNode);
}>;
export declare const Select: (props: SelectProps) => JSX.Element;
