import { jsx } from 'react/jsx-runtime';
import { createSplitProps } from '../create-split-props.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { SelectProvider } from './select-context.mjs';
import { useSelect } from './use-select.mjs';

const Select = (props) => {
  const [useSelectProps, { children }] = createSplitProps()(props, [
    "closeOnSelect",
    "defaultValue",
    "dir",
    "disabled",
    "form",
    "getRootNode",
    "highlightedOption",
    "id",
    "ids",
    "invalid",
    "loop",
    "name",
    "onChange",
    "onClose",
    "onHighlight",
    "onInteractOutside",
    "onOpen",
    "positioning",
    "readOnly",
    "selectedOption",
    "selectOnTab"
  ]);
  const select = useSelect(useSelectProps);
  const view = runIfFn(children, select);
  return /* @__PURE__ */ jsx(SelectProvider, { value: select, children: view });
};

export { Select };
