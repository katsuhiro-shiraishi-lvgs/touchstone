'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const react = require('@zag-js/react');
const select = require('@zag-js/select');
const React = require('react');
const environmentContext = require('../environment/environment-context.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const select__namespace = /*#__PURE__*/_interopNamespaceDefault(select);

const useSelect = (props) => {
  const getRootNode = environmentContext.useEnvironmentContext();
  const initialContext = {
    id: React.useId(),
    getRootNode,
    ...props,
    selectedOption: props.defaultValue
  };
  const context = {
    ...initialContext,
    selectedOption: props.selectedOption
  };
  const [state, send] = react.useMachine(select__namespace.machine(initialContext), {
    context
  });
  return select__namespace.connect(state, send, react.normalizeProps);
};

exports.useSelect = useSelect;
