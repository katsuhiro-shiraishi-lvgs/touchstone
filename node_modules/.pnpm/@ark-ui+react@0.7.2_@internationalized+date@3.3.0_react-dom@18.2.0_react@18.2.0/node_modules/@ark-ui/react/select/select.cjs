'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const createSplitProps = require('../create-split-props.cjs');
const runIfFn = require('../run-if-fn.cjs');
const selectContext = require('./select-context.cjs');
const useSelect = require('./use-select.cjs');

const Select = (props) => {
  const [useSelectProps, { children }] = createSplitProps.createSplitProps()(props, [
    "closeOnSelect",
    "defaultValue",
    "dir",
    "disabled",
    "form",
    "getRootNode",
    "highlightedOption",
    "id",
    "ids",
    "invalid",
    "loop",
    "name",
    "onChange",
    "onClose",
    "onHighlight",
    "onInteractOutside",
    "onOpen",
    "positioning",
    "readOnly",
    "selectedOption",
    "selectOnTab"
  ]);
  const select = useSelect.useSelect(useSelectProps);
  const view = runIfFn.runIfFn(children, select);
  return /* @__PURE__ */ jsxRuntime.jsx(selectContext.SelectProvider, { value: select, children: view });
};

exports.Select = Select;
