'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react = require('@zag-js/react');
const factory = require('../factory.cjs');
const forwardRef = require('../forward-ref.cjs');
const datePickerContext = require('./date-picker-context.cjs');

const DatePickerYearSelect = forwardRef.forwardRef((props, ref) => {
  const { yearSelectProps } = datePickerContext.useDatePickerContext();
  const mergedProps = react.mergeProps(yearSelectProps, props);
  return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.select, { ...mergedProps, ref, children: getYearsRange({ from: 1e3, to: 4e3 }).map((year, i) => /* @__PURE__ */ jsxRuntime.jsx("option", { value: year, children: year }, i)) });
});
function getYearsRange(range) {
  const years = [];
  for (let year = range.from; year <= range.to; year += 1) {
    years.push(year);
  }
  return years;
}

exports.DatePickerYearSelect = DatePickerYearSelect;
exports.getYearsRange = getYearsRange;
