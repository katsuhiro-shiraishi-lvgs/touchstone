'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react = require('@zag-js/react');
const factory = require('../factory.cjs');
const forwardRef = require('../forward-ref.cjs');
const runIfFn = require('../run-if-fn.cjs');
const ratingGroupContext = require('./rating-group-context.cjs');

const RatingGroupControl = forwardRef.forwardRef((props, ref) => {
  const { children, ...divProps } = props;
  const ratingGroup = ratingGroupContext.useRatingGroupContext();
  const mergedProps = react.mergeProps(ratingGroup.controlProps, divProps);
  const view = runIfFn.runIfFn(children, ratingGroup);
  return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.div, { ...mergedProps, ref, children: view });
});

exports.RatingGroupControl = RatingGroupControl;
