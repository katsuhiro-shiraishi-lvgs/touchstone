'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const radio = require('@zag-js/radio-group');
const react = require('@zag-js/react');
const React = require('react');
const environmentContext = require('../environment/environment-context.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const radio__namespace = /*#__PURE__*/_interopNamespaceDefault(radio);

const useSegmentGroup = (props) => {
  const getRootNode = environmentContext.useEnvironmentContext();
  const initialContext = {
    id: React.useId(),
    getRootNode,
    ...props,
    value: props.defaultValue
  };
  const context = {
    ...initialContext,
    value: props.value
  };
  const [state, send] = react.useMachine(radio__namespace.machine(initialContext), {
    context
  });
  return radio__namespace.connect(state, send, react.normalizeProps);
};

exports.useSegmentGroup = useSegmentGroup;
