import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { useRangeSliderContext } from './range-slider-context.mjs';

const RangeSliderMarker = forwardRef((props, ref) => {
  const { value, ...spanProps } = props;
  const { getMarkerProps } = useRangeSliderContext();
  const mergedProps = mergeProps(getMarkerProps({ value }), spanProps);
  return /* @__PURE__ */ jsx(ark.span, { ...mergedProps, ref });
});

export { RangeSliderMarker };
