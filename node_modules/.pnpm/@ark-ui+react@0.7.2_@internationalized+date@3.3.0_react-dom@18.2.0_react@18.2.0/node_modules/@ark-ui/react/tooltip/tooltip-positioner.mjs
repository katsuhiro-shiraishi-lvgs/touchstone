import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { useTooltipContext } from './tooltip-context.mjs';

const TooltipPositioner = forwardRef((props, ref) => {
  const { positionerProps, isOpen } = useTooltipContext();
  const mergedProps = mergeProps(positionerProps, props);
  return isOpen ? /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref }) : null;
});

export { TooltipPositioner };
