'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const colorPicker = require('@zag-js/color-picker');
const react = require('@zag-js/react');
const React = require('react');
const reactDom = require('react-dom');
const environmentContext = require('../environment/environment-context.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const colorPicker__namespace = /*#__PURE__*/_interopNamespaceDefault(colorPicker);

const useColorPicker = (props) => {
  const getRootNode = environmentContext.useEnvironmentContext();
  const initialContext = {
    id: React.useId(),
    getRootNode,
    ...props,
    value: props.defaultValue ?? props.value
  };
  const onChange = props.onChange;
  const onChangeWithFlushSync = React.useCallback(
    (e) => reactDom.flushSync(() => onChange?.(e)),
    [onChange]
  );
  const context = {
    ...initialContext,
    value: props.value,
    onChange: onChangeWithFlushSync
  };
  const [state, send] = react.useMachine(colorPicker__namespace.machine(initialContext), { context });
  return colorPicker__namespace.connect(state, send, react.normalizeProps);
};

exports.useColorPicker = useColorPicker;
