import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { RadioProvider } from './radio-context.mjs';
import { useRadioGroupContext } from './radio-group-context.mjs';

const Radio = forwardRef((props, ref) => {
  const { value, disabled, invalid, readOnly, ...divProps } = props;
  const { getRadioProps } = useRadioGroupContext();
  const mergedProps = mergeProps(getRadioProps({ value, disabled }), divProps);
  return /* @__PURE__ */ jsx(RadioProvider, { value: { value, disabled, invalid, readOnly }, children: /* @__PURE__ */ jsx(ark.label, { ...mergedProps, ref }) });
});

export { Radio };
