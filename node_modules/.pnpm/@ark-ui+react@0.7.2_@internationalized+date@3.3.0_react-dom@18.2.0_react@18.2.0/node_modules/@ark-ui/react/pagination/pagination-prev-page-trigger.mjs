import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { usePaginationContext } from './pagination-context.mjs';

const PaginationPrevPageTrigger = forwardRef((props, ref) => {
  const { prevPageTriggerProps } = usePaginationContext();
  const mergedProps = mergeProps(prevPageTriggerProps, props);
  return /* @__PURE__ */ jsx(ark.button, { ...mergedProps, ref });
});

export { PaginationPrevPageTrigger };
