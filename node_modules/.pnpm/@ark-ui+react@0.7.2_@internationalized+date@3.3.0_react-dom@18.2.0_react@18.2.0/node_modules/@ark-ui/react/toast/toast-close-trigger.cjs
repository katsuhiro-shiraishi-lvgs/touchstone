'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react = require('@zag-js/react');
const factory = require('../factory.cjs');
const forwardRef = require('../forward-ref.cjs');
const toastItemContext = require('./toast-item-context.cjs');

const ToastCloseTrigger = forwardRef.forwardRef((props, ref) => {
  const { closeTriggerProps } = toastItemContext.useToastItemContext();
  const mergedProps = react.mergeProps(closeTriggerProps, props);
  return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.button, { ...mergedProps, ref });
});

exports.ToastCloseTrigger = ToastCloseTrigger;
