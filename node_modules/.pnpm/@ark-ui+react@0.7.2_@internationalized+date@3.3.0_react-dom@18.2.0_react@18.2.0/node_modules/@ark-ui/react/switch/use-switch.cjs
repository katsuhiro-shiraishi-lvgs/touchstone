'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const react = require('@zag-js/react');
const zagSwitch = require('@zag-js/switch');
const React = require('react');
const environmentContext = require('../environment/environment-context.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const zagSwitch__namespace = /*#__PURE__*/_interopNamespaceDefault(zagSwitch);

const useSwitch = (props) => {
  const getRootNode = environmentContext.useEnvironmentContext();
  const context = {
    id: React.useId(),
    getRootNode,
    ...props
  };
  const [state, send] = react.useMachine(zagSwitch__namespace.machine(context), { context });
  return zagSwitch__namespace.connect(state, send, react.normalizeProps);
};

exports.useSwitch = useSwitch;
