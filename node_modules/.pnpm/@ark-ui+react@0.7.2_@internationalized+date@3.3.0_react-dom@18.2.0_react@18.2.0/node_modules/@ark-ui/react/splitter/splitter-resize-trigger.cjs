'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react = require('@zag-js/react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const forwardRef = require('../forward-ref.cjs');
const splitterContext = require('./splitter-context.cjs');

const SplitterResizeTrigger = forwardRef.forwardRef(
  (props, ref) => {
    const [triggerProps, restProps] = createSplitProps.createSplitProps()(props, [
      "disabled",
      "id",
      "step"
    ]);
    const { getResizeTriggerProps } = splitterContext.useSplitterContext();
    const mergedProps = react.mergeProps(getResizeTriggerProps(triggerProps), restProps);
    return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.button, { ref, ...mergedProps });
  }
);

exports.SplitterResizeTrigger = SplitterResizeTrigger;
