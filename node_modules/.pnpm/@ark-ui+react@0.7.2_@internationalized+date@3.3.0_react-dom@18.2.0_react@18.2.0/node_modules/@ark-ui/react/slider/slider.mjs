import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { runIfFn } from '../run-if-fn.mjs';
import { SliderProvider } from './slider-context.mjs';
import { useSlider } from './use-slider.mjs';

const Slider = forwardRef((props, ref) => {
  const { children, ...restProps } = props;
  const [useSliderProps, divProps] = createSplitProps()(restProps, [
    "aria-label",
    "aria-labelledby",
    "defaultValue",
    "dir",
    "disabled",
    "focusThumbOnChange",
    "form",
    "getAriaValueText",
    "getRootNode",
    "id",
    "ids",
    "invalid",
    "max",
    "min",
    "name",
    "onChange",
    "onChangeEnd",
    "onChangeStart",
    "orientation",
    "origin",
    "readOnly",
    "step",
    "thumbAlignment",
    "value"
  ]);
  const slider = useSlider(useSliderProps);
  const mergedProps = mergeProps(slider.rootProps, divProps);
  const view = runIfFn(children, slider);
  return /* @__PURE__ */ jsx(SliderProvider, { value: slider, children: /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref, children: view }) });
});

export { Slider };
