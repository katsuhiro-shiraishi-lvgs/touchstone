'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react = require('@zag-js/react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const forwardRef = require('../forward-ref.cjs');
const runIfFn = require('../run-if-fn.cjs');
const sliderContext = require('./slider-context.cjs');
const useSlider = require('./use-slider.cjs');

const Slider = forwardRef.forwardRef((props, ref) => {
  const { children, ...restProps } = props;
  const [useSliderProps, divProps] = createSplitProps.createSplitProps()(restProps, [
    "aria-label",
    "aria-labelledby",
    "defaultValue",
    "dir",
    "disabled",
    "focusThumbOnChange",
    "form",
    "getAriaValueText",
    "getRootNode",
    "id",
    "ids",
    "invalid",
    "max",
    "min",
    "name",
    "onChange",
    "onChangeEnd",
    "onChangeStart",
    "orientation",
    "origin",
    "readOnly",
    "step",
    "thumbAlignment",
    "value"
  ]);
  const slider = useSlider.useSlider(useSliderProps);
  const mergedProps = react.mergeProps(slider.rootProps, divProps);
  const view = runIfFn.runIfFn(children, slider);
  return /* @__PURE__ */ jsxRuntime.jsx(sliderContext.SliderProvider, { value: slider, children: /* @__PURE__ */ jsxRuntime.jsx(factory.ark.div, { ...mergedProps, ref, children: view }) });
});

exports.Slider = Slider;
