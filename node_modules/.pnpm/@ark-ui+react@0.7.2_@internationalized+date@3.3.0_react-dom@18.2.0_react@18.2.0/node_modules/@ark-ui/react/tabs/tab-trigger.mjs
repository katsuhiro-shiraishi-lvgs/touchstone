import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { createSplitProps } from '../create-split-props.mjs';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { useTabsContext } from './tabs-context.mjs';

const TabTrigger = forwardRef((props, ref) => {
  const [tabProps, buttonProps] = createSplitProps()(props, ["disabled", "value"]);
  const { getTriggerProps } = useTabsContext();
  const mergedProps = mergeProps(getTriggerProps(tabProps), buttonProps);
  return /* @__PURE__ */ jsx(ark.button, { ...mergedProps, ref });
});

export { TabTrigger };
