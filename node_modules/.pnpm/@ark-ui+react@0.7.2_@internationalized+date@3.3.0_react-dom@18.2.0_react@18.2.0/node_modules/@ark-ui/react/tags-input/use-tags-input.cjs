'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const react = require('@zag-js/react');
const tagsInput = require('@zag-js/tags-input');
const React = require('react');
const environmentContext = require('../environment/environment-context.cjs');

function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: 'Module' } });
  if (e) {
    for (const k in e) {
      if (k !== 'default') {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}

const tagsInput__namespace = /*#__PURE__*/_interopNamespaceDefault(tagsInput);

const useTagsInput = (props) => {
  const getRootNode = environmentContext.useEnvironmentContext();
  const initialContext = {
    id: React.useId(),
    getRootNode,
    ...props,
    value: props.value ?? props.defaultValue
  };
  const context = {
    ...initialContext,
    value: props.value
  };
  const [state, send] = react.useMachine(tagsInput__namespace.machine(initialContext), { context });
  return tagsInput__namespace.connect(state, send, react.normalizeProps);
};

exports.useTagsInput = useTagsInput;
