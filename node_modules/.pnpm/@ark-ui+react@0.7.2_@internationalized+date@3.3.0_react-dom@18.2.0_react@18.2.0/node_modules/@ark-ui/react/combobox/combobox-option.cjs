'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const jsxRuntime = require('react/jsx-runtime');
const react = require('@zag-js/react');
const createSplitProps = require('../create-split-props.cjs');
const factory = require('../factory.cjs');
const forwardRef = require('../forward-ref.cjs');
const comboboxContext = require('./combobox-context.cjs');

const ComboboxOption = forwardRef.forwardRef((props, ref) => {
  const [optionProps, { children, ...liProps }] = createSplitProps.createSplitProps()(props, [
    "count",
    "disabled",
    "label",
    "value",
    "index"
  ]);
  const { getOptionProps } = comboboxContext.useComboboxContext();
  const mergedProps = react.mergeProps(getOptionProps(optionProps), liProps);
  return /* @__PURE__ */ jsxRuntime.jsx(factory.ark.li, { ...mergedProps, ref, children: children ? children : optionProps.label });
});

exports.ComboboxOption = ComboboxOption;
