import { createAnatomy } from '@zag-js/anatomy';

const anatomy = createAnatomy("dialog").parts(
  "trigger",
  "backdrop",
  "container",
  "content",
  "title",
  "description",
  "closeTrigger"
);
const parts = anatomy.build();

export { anatomy, parts };
