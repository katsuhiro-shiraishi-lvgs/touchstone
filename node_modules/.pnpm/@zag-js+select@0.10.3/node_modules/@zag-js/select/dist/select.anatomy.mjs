import { createAnatomy } from '@zag-js/anatomy';

const anatomy = createAnatomy("select").parts(
  "label",
  "positioner",
  "trigger",
  "option",
  "optionGroup",
  "optionGroupLabel",
  "hiddenSelect",
  "content"
);
const parts = anatomy.build();

export { anatomy, parts };
