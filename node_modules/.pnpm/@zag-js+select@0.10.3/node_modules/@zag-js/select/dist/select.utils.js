'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const utils = require('@zag-js/utils');

function isOptionData(v) {
  return utils.isObject(v) && "value" in v && "label" in v;
}
function validateOptionData(v) {
  if (!isOptionData(v)) {
    throw new TypeError("Expected an option data object");
  }
}

exports.isOptionData = isOptionData;
exports.validateOptionData = validateOptionData;
