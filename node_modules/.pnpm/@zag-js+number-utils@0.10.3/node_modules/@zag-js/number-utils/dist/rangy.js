'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const number = require('./number.js');

function rangy(o) {
  const wrap = (v) => rangy({ ...o, value: v });
  return {
    isInRange: number.isWithinRange(o.value, o),
    isAtMax: number.isAtMax(o.value, o),
    isAtMin: number.isAtMin(o.value, o),
    percent: number.valueToPercent(o.value, o),
    valueAsNumber: number.valueOf(o.value),
    value: number.formatDecimal(o.value, o),
    snapToStep: () => wrap(number.snapToStep(o.value, o.step)),
    increment: (s) => wrap(number.increment(o.value, s || o.step)),
    decrement: (s) => wrap(number.decrement(o.value, s || o.step)),
    toMax: () => wrap(o.max),
    toMin: () => wrap(o.min),
    fromPercent: (p) => wrap(number.percentToValue(p, o)),
    clamp: () => wrap(number.clamp(o.value, o))
  };
}

exports.rangy = rangy;
