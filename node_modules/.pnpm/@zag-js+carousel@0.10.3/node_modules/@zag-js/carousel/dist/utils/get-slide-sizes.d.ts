import { MachineContext } from "../carousel.types";
export type SlideSizesType = {
    slideSizes: number[];
    slideSizesWithGaps: number[];
};
export declare function getSlideSizes(ctx: MachineContext): SlideSizesType;
