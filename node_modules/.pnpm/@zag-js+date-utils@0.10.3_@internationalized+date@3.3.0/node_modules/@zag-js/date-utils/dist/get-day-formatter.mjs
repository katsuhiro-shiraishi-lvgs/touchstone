import { toCalendarDateTime, today, DateFormatter } from '@internationalized/date';
import { getEraFormat } from './get-era-format.mjs';

function getDayFormatter(locale, timeZone) {
  const date = toCalendarDateTime(today(timeZone));
  return new DateFormatter(locale, {
    weekday: "long",
    month: "long",
    year: "numeric",
    day: "numeric",
    era: getEraFormat(date),
    timeZone
  });
}

export { getDayFormatter };
