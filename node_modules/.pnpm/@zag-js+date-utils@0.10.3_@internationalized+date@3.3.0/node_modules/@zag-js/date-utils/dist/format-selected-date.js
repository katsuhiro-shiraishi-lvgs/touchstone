'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const date = require('@internationalized/date');
const formatRange = require('./format-range.js');
const getDayFormatter = require('./get-day-formatter.js');

function formatSelectedDate(startDate, endDate, locale, timeZone) {
  let start = startDate;
  let end = endDate ?? startDate;
  let formatter = getDayFormatter.getDayFormatter(locale, timeZone);
  if (date.isSameDay(start, end)) {
    return formatter.format(start.toDate(timeZone));
  }
  return formatRange.formatRange(start, end, formatter, (start2, end2) => `${start2} – ${end2}`, timeZone);
}

exports.formatSelectedDate = formatSelectedDate;
